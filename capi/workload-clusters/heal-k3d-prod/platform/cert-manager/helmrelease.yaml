apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  interval: 30m
  chart:
    spec:
      chart: cert-manager
      version: "v1.19.6"
      sourceRef:
        kind: HelmRepository
        name: jetstack
        namespace: flux-system
      interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      remediateLastFailure: true
    cleanupOnFail: true
  # Reverts hand edits to the release on the next reconcile.
  driftDetection:
    mode: enabled
  values:
    # cert-manager does not install its own CRDs unless asked, and without them
    # no Certificate or Issuer ever reconciles.
    crds:
      enabled: true
    # Two replicas: prod has three workers, so the chart's default webhook
    # anti-affinity is satisfiable here in a way it is not on dev.
    replicaCount: 2
    webhook:
      replicaCount: 2
    cainjector:
      replicaCount: 1
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        memory: 256Mi
